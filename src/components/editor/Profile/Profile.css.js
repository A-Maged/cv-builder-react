import { css } from 'emotion';

export const profile = css`
   .details {
      cursor: pointer;
      padding: 15px;
      margin-bottom: 10px;
      box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.2);
   }
`;
